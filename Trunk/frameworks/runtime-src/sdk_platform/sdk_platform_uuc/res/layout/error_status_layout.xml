<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/white" >

    <TextView
        android:id="@+id/error_status_text_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btn_retry"
        android:layout_centerInParent="true"
        android:lineSpacingExtra="3dip"
        android:text="@string/no_networking_text"
        android:textColor="@color/color_network_error_text"
        android:textSize="13sp" />

    <Button
        android:id="@+id/btn_retry"
        android:layout_width="@dimen/sdk_refresh_btn_width"
        android:layout_height="@dimen/sdk_refresh_btn_height"
        android:layout_centerInParent="true"
        android:layout_marginTop="10dp"
        android:background="@drawable/sdk_btn_refresh_selector"
        android:text="@string/click_to_refresh"
        android:textColor="@color/color_bottom_btn_selected"
        android:textSize="13sp"
        android:visibility="gone" />

</RelativeLayout>